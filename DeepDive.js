function ScrollToUdstyr() {
    const udstyrSection = document.getElementById('udstyrBaggrund');
    udstyrSection.scrollIntoView({ behavior: 'smooth' });
}

function ScrollToKurser() {
    const kurserSection = document.getElementById('kurserBaggrund');
    kurserSection.scrollIntoView({ behavior: 'smooth' });
}

function ScrollToKontakt() {
    const kontaktSection = document.getElementById('kontakt');
    kontaktSection.scrollIntoView({ behavior: 'smooth' });
}
